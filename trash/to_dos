
1. form validation in login and registration pages for consumer and seller.
2. fixed width and floating header need to be revisited.
3. fix the links if external reference rel="external"

$list_array = array();

while($details = mysql_fetch_array($result))
{	
	$array = array(
		"message" => $details["message"],
		"sender" => $details["sender"],
		"time" => $details["time"]
	);
	array_push($list_array, $array);
}

echo json_encode($list_array);
